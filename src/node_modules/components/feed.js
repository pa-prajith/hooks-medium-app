import React, { Fragment } from 'react'
import FeedItem from 'components/feeditem'

const Feed = ({articles}) => {
    return (
        <Fragment>
           {articles.length && articles.map((a,itx) => (
                <FeedItem article={a} key={itx} />
            )) }
        </Fragment>
    )
}

export default Feed
