import React from 'react'

const TagList = ({article}) => {
    return (
        <ul className="feed-tag">
            {
                article.taglist && article.taglist.map(t => (<li>{t}</li>))
            }
        </ul>
    )
}

export default TagList
