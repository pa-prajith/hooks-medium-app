import React from 'react'

const TagList = ({article}) => {
    return (
        <ul className="feed-tag">
            {
                article.tagList && article.tagList.map((t,i) => (<li key={i}>{t}</li>))
            }
        </ul>
    )
}

export default TagList
