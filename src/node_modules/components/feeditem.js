import React from 'react'
import {Link} from 'react-router-dom'
import FeedItemUser from 'components/feeditemuser'
import TagList from 'components/taglist'
import Favorite from 'components/favorite'

const FeedItem = ({article}) => {
    return (
        <div className="feed-item">
            <FeedItemUser article={article} />
            <div className="feed-item-content">
                <div className="feed-content-header">
                    <h3>{article.title}</h3>
                    <Favorite isFavorite={article.favorited} favoriteCount={article.favoritesCount} slug={article.slug} />
                </div>
                <div className="feed-content">
                    {article.description}
                </div>
                <div className="feed-content-details">
                    <Link className="feed-readmore" to={`/articles/${article.slug}`}>Readmore ...</Link>
                    <TagList article={article} />
                </div>
            </div>
        </div>
    )
}

export default FeedItem
