import React from 'react';

export default ({errorMessages}) => {
        const messages = Object.keys(errorMessages).map((key,itx) => {
            return (<li key={itx}>{key} {errorMessages[key].join(' ')}</li>)
        })
    return (
        <ul id="error-messages">
            {messages}
        </ul>
    )
}