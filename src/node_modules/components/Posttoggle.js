import React, {useContext} from 'react'
import {NavLink} from 'react-router-dom'

const PostToggle = ({slug}) => {
    return (
        <div className="feed-toggler">
            <ul className="toggle-items">
                <li className="toggle-item">
                    <NavLink to={`/profile/${slug}`} exact>My Posts</NavLink>
                </li>
                <li className="toggle-item">
                    <NavLink to={`/profile/${slug}/favorites`} exact>My Favorites</NavLink>
                </li>
                
            </ul>
        </div>
    )
}

export default PostToggle
